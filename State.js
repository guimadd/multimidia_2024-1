class State {
    constructor(output, dictionary, position, word, symbol) {
        this.output = output;
        this.dictionary = dictionary;
        this.position = position;
        this.word = word;
        this.symbol = symbol;
    }
}